import { StyleSheet } from "react-native";

export const PADDING = 16;
export const HEADING_BUTTON_SIZE = 24;
export const BUTTON_SIZE = 20;

const styles = StyleSheet.create({
  buttonIcon: {
    padding: PADDING,
  },
});

export const buttonIcon = styles.buttonIcon;
